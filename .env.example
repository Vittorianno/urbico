# Copie este arquivo para ".env" e preencha com seus próprios valores.
# NUNCA coloque valores reais deste arquivo no Git — .env já está no .gitignore.

# ---------------------------------------------------------------------------
# Backend / servidor (server/_core/env.ts)
# ---------------------------------------------------------------------------

# Porta do servidor Express. Se estiver ocupada, o servidor sobe automaticamente
# na próxima porta livre (3000, 3001, 3002...).
PORT=3000

# String de conexão MySQL usada pelo Drizzle ORM e por "pnpm db:push".
# Sem isto configurado, o app ainda inicia — ele apenas roda sem persistência
# (login não é salvo, alertas de saída e relatos de lotação não funcionam).
DATABASE_URL=mysql://usuario:senha@localhost:3306/urbico

# Segredo usado para assinar o cookie/token de sessão (JWT) do Urbico. Gere
# uma string aleatória longa e única por ambiente, ex.: `openssl rand -base64 48`.
# Independente do provedor de login (Supabase) — este segredo é só do Urbico.
# Também assina o "state" do OAuth do Google Agenda (ver seção abaixo).
JWT_SECRET=

# ---------------------------------------------------------------------------
# Autenticação (Supabase Auth) — server/_core/sdk.ts, server/_core/supabaseAuth.ts
# ---------------------------------------------------------------------------
# O app faz login/cadastro direto contra o Supabase (cliente
# @supabase/supabase-js), e troca o access token resultante por uma sessão
# própria do Urbico em POST /api/auth/session. Veja o README ("Autenticação").

# URL do projeto Supabase, ex.: https://xxxxxxxx.supabase.co
SUPABASE_URL=

# Chave pública (anon/publishable) do projeto Supabase — não é um segredo,
# é a mesma chave usada no cliente. Painel do Supabase → Project Settings → API.
SUPABASE_ANON_KEY=

# openId (agora no formato "supabase:<uuid-do-usuário>") do dono/administrador
# do projeto, usado para conceder o papel "admin". Preencha depois do
# primeiro cadastro real, consultando a tabela `users` do banco.
OWNER_OPEN_ID=

# ---------------------------------------------------------------------------
# SPTrans Olho Vivo — server/integrations/sptrans.ts
# ---------------------------------------------------------------------------
# Token de desenvolvedor obtido em http://www.sptrans.com.br/desenvolvedores/
# Fica só no backend — nunca é enviado ao cliente/app.
SPTRANS_TOKEN=

# ---------------------------------------------------------------------------
# Geocodificação e rotas a pé — server/integrations/open-geospatial.ts
# ---------------------------------------------------------------------------
# Ambos opcionais. Sem eles configurados, o app usa serviços públicos
# gratuitos como alternativa automática — Nominatim (OpenStreetMap) para
# busca de endereço e o servidor de demonstração do OSRM para rota a pé —
# então a busca de endereço e o cálculo de rota funcionam de verdade mesmo
# sem preencher nada aqui. Preencha só se quiser rodar sua própria instância
# (mais controle/robustez para uso em produção com volume).

# URL de uma instância própria do Pelias (geocodificação / busca de endereço).
PELIAS_BASE_URL=

# URL de uma instância própria do Valhalla (cálculo de rota a pé).
VALHALLA_BASE_URL=

# ---------------------------------------------------------------------------
# Google Agenda — server/integrations/google-calendar.ts
# ---------------------------------------------------------------------------
# Opcional. Sem isto configurado, o botão "Conectar Google Agenda" no Perfil
# mostra um erro claro em vez de fingir que funciona — nenhuma outra parte do
# Urbico depende disso.
#
# Como obter:
#   1. https://console.cloud.google.com/ → crie (ou reutilize) um projeto.
#   2. "APIs e serviços" → "Biblioteca" → ative a "Google Calendar API".
#   3. "APIs e serviços" → "Tela de consentimento OAuth" → configure (modo
#      "Externo" está OK para uso pessoal; adicione seu e-mail em "Usuários
#      de teste" enquanto o app não estiver publicado/verificado).
#   4. "Credenciais" → "Criar credenciais" → "ID do cliente OAuth" → tipo
#      "Aplicativo da Web".
#   5. Em "URIs de redirecionamento autorizados", adicione EXATAMENTE a
#      mesma URL que você colocar em GOOGLE_REDIRECT_URI abaixo — precisa
#      ser uma URL pública e estável (não aceita localhost em produção, e
#      não pode ser um esquema urbico://, isso é tratado depois, só no
#      redirecionamento final de volta para o app).

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Ex.: https://seu-dominio-do-backend.com/api/google-calendar/callback
GOOGLE_REDIRECT_URI=

# ---------------------------------------------------------------------------
# Norby (assistente) — server/integrations/norby.ts
# ---------------------------------------------------------------------------
# Ambos opcionais. Sem eles configurados, o Norby usa respostas locais
# baseadas em regras (lib/urbico-logic.ts) em vez de um modelo de linguagem.

# URL base de uma instância do Ollama (ex.: http://localhost:11434).
OLLAMA_BASE_URL=

# Nome do modelo Ollama a usar (ex.: llama3.1).
OLLAMA_MODEL=

# ---------------------------------------------------------------------------
# Monitoramento de alertas via cron HTTP externo (opcional) —
# server/scheduled/departure-alerts.ts
# ---------------------------------------------------------------------------
# Só necessário se você hospedar em um ambiente sem processo de longa duração
# (serverless) e precisar de um cron externo (GitHub Actions, Vercel Cron)
# acionando POST /api/scheduled/monitor-departure-alerts com o header
# "x-cron-secret". O agendador interno já cobre hospedagem com processo
# contínuo — sem CRON_SECRET configurado, esse endpoint HTTP fica desativado.
CRON_SECRET=

# ---------------------------------------------------------------------------
# Legado do template Manus — opcional, só necessário se você ainda usa esses
# recursos específicos da plataforma Manus (notificação ao dono via
# server/_core/notification.ts, geração de imagem, transcrição de voz,
# proxy de armazenamento). Nenhuma funcionalidade do Urbico em uso depende
# destas variáveis hoje. Veja o README ("Dependências legadas") antes de
# remover os arquivos correspondentes.
# ---------------------------------------------------------------------------
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# ---------------------------------------------------------------------------
# Cliente Expo (variáveis EXPO_PUBLIC_* — vão para o bundle, nunca coloque
# segredos aqui). Normalmente preenchidas automaticamente a partir das
# variáveis do servidor acima por scripts/load-env.js; defina manualmente só
# se precisar de um valor diferente no cliente.
# ---------------------------------------------------------------------------

# Porta do Metro bundler (padrão 8081).
EXPO_PORT=8081

# URL base da API que o app (web/Android/iOS) deve chamar. Em desenvolvimento
# web costuma ser derivada automaticamente do hostname; defina explicitamente
# para builds nativos e produção, ex.: https://api.seudominio.com
EXPO_PUBLIC_API_BASE_URL=

# Mesmo valor de SUPABASE_URL/SUPABASE_ANON_KEY acima, expostos ao cliente
# (são chaves públicas — o cliente @supabase/supabase-js precisa delas para
# fazer login/cadastro diretamente contra o Supabase).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
